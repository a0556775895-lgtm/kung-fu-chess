# Kung Fu Chess

משחק שחמט בזמן אמת: אין תורות, ושני הצדדים יכולים להזיז כלים בו-זמנית. כל תנועה נמשכת זמן בהתאם למרחק, ובזמן הזה היריב יכול להגיב, לנוע ולקפוץ.

הפרויקט פועל במבנה Multiplayer: שני לקוחות גרפיים מחוברים לשרת WebSocket סמכותי.

## תכונות עיקריות

- **תנועה בזמן אמת** — כל הכלים יכולים לנוע בו-זמנית. משך התנועה תלוי במרחק, למעט פרש שנע במשך זמן קבוע.
- **קפיצה** — קליק ימני שולח כלי לקפיצה זמנית. בזמן הקפיצה הוא חסין לתפיסה רגילה, אך עדיין יכול להיפגע מכלי שמגיע לאותה משבצת.
- **מנוחה (cooldown)** — לאחר מהלך או קפיצה הכלי ממתין לפני שיוכל לפעול שוב.
- **הכתרת רגלי** — רגלי שמגיע לשורה האחרונה הופך למלכה.
- **ניקוד סמכותי** — השרת מחשב את הניקוד לפי הכלים שנתפסו: רגלי=1, פרש/רץ=3, צריח=5 ומלכה=9. תפיסת המלך מסיימת את המשחק.
- **יומן מהלכים** — מהלכים שהושלמו מוצגים בסימון אלגברי עם חותמת זמן.
- **Multiplayer** — שני לקוחות חולקים משחק אחד ומקבלים מהשרת את אותו מצב לוח.
- **חשבונות משתמשים** — הרשמה וכניסה עם שם משתמש וסיסמה, הנשמרים ב-SQLite בצד השרת. שם שכבר מחובר אינו יכול להתחבר שוב.
- **שמות שחקנים** — שמות השחקנים מוצגים בפאנלים לצד הניקוד במקום הכיתובים White ו-Black.
- **שרת סמכותי** — הלקוח שולח בקשות בלבד; חוקיות המהלכים, הזמן, ההתנגשויות, התפיסות והניקוד נקבעים בשרת.
- **חוקי שחמט בסיסיים** — תנועה וחסימה לפי סוג הכלי, ללא check, checkmate, castling או en passant.

## דרישות והתקנה

- Python 3.13 או גרסה תואמת.
- OpenCV, NumPy, WebSockets ו-pytest — מפורטים ב-`requirements.txt`.

מתוך תיקיית הפרויקט:

```powershell
pip install -r requirements.txt
```

## הרצת משחק מרובה משתתפים

למשחק רגיל יש לפתוח שלושה חלונות PowerShell מתוך תיקיית הפרויקט: שרת ושתי לקוחות. לבדיקת מצב צפייה פותחים לקוחה רביעית.

בחלון הראשון מפעילים את השרת:

```powershell
python -m server.main
```

בחלון השני מפעילים את הלקוח הראשון:

```powershell
python -m client.main
```

בחלון השלישי מפעילים את הלקוח השני:

```powershell
python -m client.main
```

בכל לקוחה בוחרים `Login` או `Register`, ואז מזינים שם משתמש וסיסמה. בהרצה הראשונה יש לרשום חשבונות שונים; בהרצות הבאות ניתן להתחבר אליהם. סיסמה חדשה חייבת להכיל לפחות 4 תווים.

לאחר האימות נפתח Lobby גרפי. שתי הלקוחות הראשונות יכולות להשתמש ב-Quick Match או ליצור חדר ולהצטרף אליו באמצעות הקוד. לקוחה שלישית שמצטרפת לאותו חדר לאחר תחילת המשחק נכנסת כצופה: היא מקבלת את מצב המשחק בזמן אמת, אך אינה יכולה לבצע מהלכים.

כדי להתחבר לשרת בכתובת אחרת:

```powershell
python -m client.main --server ws://127.0.0.1:8765
```

## שליטה

- **קליק שמאלי** — בחירת כלי, ולאחר מכן בחירת משבצת היעד.
- **קליק ימני על כלי** — קפיצה.
- **Esc** — סגירת חלון המשחק.

## תהליך ההתחברות

לפני שהלקוח יכול לשלוח פקודות משחק הוא משלים handshake מסודר:

```text
REGISTER או LOGIN
  ↓
AuthService + SQLite
  ↓
AUTH_OK
  ↓
JOIN עם הקונפיג המבוקש
  ↓
CONFIG_ACCEPTED או CONFIG_OVERRIDDEN
  ↓
STATE ראשוני
```

לאחר מכן הלקוח שולח פקודות `MOVE` ו-`JUMP`. השרת מאמת הרשאות וחוקיות ומפיץ בחזרה הודעות `STATE`, ‏`EVENT`, ‏`OK` או `ERR`.

## מבנה הפרויקט

```text
client/         הלקוח הגרפי
  assets/       תמונות, ספרייטים וצלילים
  input/        מיפוי קלט ו-InputController
  transport/    חיבור WebSocket ומתאמי אירועים
  view/         חלון OpenCV, אנימציה, לוח ו-HUD

server/         השרת הסמכותי
  boardio/      יצירת לוחות וטעינת קונפיגורציה
  dal/          SQLite, repositories ו-DTOs
  engine/       GameEngine
  game/         Match, הרשאות ולולאת tick
  models/       מצב הלוח והמשחק בצד השרת
  realtime/     תנועות פעילות, הגעות והתנגשויות
  rules/        חוקיות תנועה וחישובי מסלול
  services/     אימות משתמשים ושמות פעילים
  transport/    WebSocket, חיבורים ותורי הודעות

networking/     כל מה שמשותף ללקוח ולשרת
  models/       מודלים משותפים וקונפיגורציית משחק
  protocols/    חוזי תקשורת
  serializers/  המרה להודעות רשת ומהן
  event_bus.py  הפצת אירועים בתוך התהליך
  events.py     אירועים משותפים

DOCS/           תיעוד ובדיקות
  config/       הגדרות כלי פיתוח וכיסוי
  plans/        מסמכי תכנון
  tests/        בדיקות יחידה, אינטגרציה ותרחישי טקסט
  run_tests.ps1 הרצת הבדיקות עם כיסוי
```

בשורש המאגר נשארים רק ארבעת העצים האלה וקובצי הפרויקט הכלליים: `README.md`, ‏`CLAUDE.md`, ‏`requirements.txt` ו-`.gitignore`. קובצי runtime כגון מסד הנתונים, לוגים, מטמונים ודוחות בדיקה נוצרים בתוך התיקייה המתאימה ומוחרגים מ-Git.

לפירוט ארכיטקטורת התצוגה ראו [client_view.md](DOCS/plans/client_view.md). לתוכנית הפיתוח המלאה ראו [server_plan.md](DOCS/plans/server_plan.md).

## ארכיטקטורת Multiplayer

```text
קלט עכבר
   ↓
RemoteGameEngineProxy
   ↓
NetworkClient
   ↓ WebSocket
GameServer
   ↓
GameAdmission / GameController
   ↓
Match
   ↓
GameEngine
   ↓
EventBus + ServerBroadcaster
   ↓ STATE / EVENT
NetworkClient → Proxy → View
```

הלקוח אינו משנה את מצב המשחק בעצמו. הוא מציג snapshots מהשרת ושולח בקשות לפעולות. רק השרת מקדם את שעון המשחק ומכריע תנועה, הגעה, תפיסה, קפיצה, מנוחה וניקוד.

`client.main` מחבר את `DisplayManager` ל-`RemoteGameEngineProxy`; התצוגה מציגה את המצב שמתקבל מהשרת ואינה מריצה מנוע משחק מקומי.

## לוגים

בהרצת multiplayer נוצרים קובצי לוג מתחלפים:

- `server/logs/server.log` — מחזור החיים של השרת, אימותים, חדרים וחיבורים.
- `client/logs/client_<username>.log` — פעילות של לקוחה מסוימת.
- `server/logs/games/game_<game_id>.log` — חיבורים, פקודות, אירועי משחק וסיום של Match יחיד.

כל קובץ מוגבל ל־1MB ונשמרים עד שלושה גיבויים. הודעות אימות גולמיות אינן נכתבות, וסיסמאות ו־session tokens עוברים redaction נוסף. קובצי הלוג והגיבויים אינם נכנסים ל־Git.

## בדיקות

הרצת כל בדיקות היחידה והאינטגרציה:

```powershell
python -m pytest DOCS/tests -q
```

הרצה עם דוח כיסוי:

```powershell
.\DOCS\run_tests.ps1
```

בנקודת העדכון האחרונה עברו 562 בדיקות ב-100% כיסוי של הלוגיקה הנמדדת. הבדיקות כוללות חוקי משחק, serialization, הרשאות, WebSocket אמיתי, הרשמה וכניסה מול SQLite, חדרים, matchmaking, reconnect, צפייה ולוגים מתחלפים ללא מידע רגיש.

## מצב הפיתוח

- **שלב A — EventBus:** הושלם.
- **שלב B — Multiplayer ושרת סמכותי:** הושלם.
- **שלב C — כניסה בשם משתמש:** הושלם.
- **שלב D — סיסמאות, SQLite ו-ELO:** הושלם.
- **שלב E — Matchmaking, ניתוקים ו-Reconnect:** הושלם.
- **שלב F — חדרים, Lobby, צופות ולוגים:** הושלם ואושר.

הסטטוס המפורט ותתי-השלבים נמצאים ב-[server_plan.md](DOCS/plans/server_plan.md).
